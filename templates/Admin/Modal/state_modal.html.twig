<div class="modal fade" id="stateModal" tabindex="-1" aria-labelledby="stateLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="stateLabel">Guide sur les états</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                {% for state in states %}
                    <span class="title-state">{{ state.title }}</span>
                    <p class="description-state">{{ state.description }}</p>
                {% endfor %}
            </div>
        </div>
    </div>
</div>