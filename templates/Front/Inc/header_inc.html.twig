<!-- Main Header Nav -->
<header class="header-nav menu_style_home_one navbar-scrolltofixed stricky main-menu">
    <div class="container">
        <!-- Ace Responsive Menu -->
        <nav class="nav-bdm">
            <!-- Menu Toggle btn-->
            <div class="menu-toggle">
                <img class="nav_logo_img img-fluid" src="{{ asset('front/images/logo.png') }}" alt="logo">
                <button type="button" id="menu-btn">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <a href="{{ path('home_bdmk') }}" class="navbar_brand float-left dn-smd">
                <img class="logo1 " src="{{ asset('front/images/logo.png') }}" alt="logo">
                <img class="logo2 " src="{{ asset('front/images/logo.png') }}" alt="logo">
            </a>
            <!-- Responsive Menu Structure-->
            <!--Note: declare the Menu style in the data-menu-style="horizontal" (options: horizontal, vertical, accordion) -->
            <ul id="respMenu" class="ace-responsive-menu text-right" data-menu-style="horizontal">
                <li>
                    <a href="{{ path('home_bdmk') }}"><span class="title"><i class="fa fa-home"></i> Accueil</span></a>
                </li>
                <li>
                    <a href="{{ path('list_search_annonces_bdmk') }}"><span class="title"><i class="fa fa-search"></i> Rechercher </span></a>
                </li>
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <li>
                    <a href="{{ path('login_bdmk') }}"><span class="title"><i class="fa fa-heart"></i> Favoris</span></a>
                </li>
                {% else %}
                <li>
                    <a href="{{ path('admin_favoris_liste_bdmk') }}"><span class="title"><i class="fa fa-heart"></i> Favoris</span></a>
                </li>
                {% endif %}
                <li>
                    <a href="{{ path('admin_annnonce_add_bdmk') }}"><span class="give-annonce"><i class="fa fa-plus-circle"></i>Déposer une annonce</span></a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% if app.user.avatar %}
                        {% if app.user.avatar.fileName is null %}
                            {% set imageUrl = '/front/images/avatar.png' %}
                        {% else %}
                            {% set imageUrl = asset('/front/webfiles/' ~ app.user.avatar.fileName) %}
                        {% endif %}
                    {% else %}
                        {% set imageUrl = '/front/images/avatar.png' %}
                    {% endif %}
                    <li>
                        <div class="img-profile" style="background-image: url('{{ imageUrl }}');"></div>
                    </li>
                    <li class="dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                            {% if app.user.firstname %}<i class="fa fa-lightbulb-o" aria-hidden="true"></i>{{ app.user.firstname }}{% else %}{{ app.user.email }}{% endif %}
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('admin_dashboard_bdmk') }}"><i class="fa fa-dashboard"></i>Tableau de bord</a></li>
                            {% if is_granted('ROLE_USER')  %}
                            <li><a class="dropdown-item" href="{{ path('edit_profile_admin_bdmk') }}"><i class="fa fa-user-circle"></i> Mon profil</a></li>
                            {% elseif is_granted('ROLE_PRO') %}
                            <li><a class="dropdown-item" href="{{ path('edit_profile_pro_admin_bdmk') }}"><i class="fa fa-cog"></i> Paramètres</a></li>
                            {% endif %}
                            <li><a class="dropdown-item" href="{{ path('reset_password_bdmk') }}"><i class="fa fa-lock"></i> Mot de passe</a></li>
                            <li><a class="dropdown-item" href="{{ path('admin_annnonce_liste_bdmk') }}"><i class="fa fa-bars"></i>Mes annonces</a></li>
                            <li><a class="dropdown-item" href="{{ path('admin_favoris_liste_bdmk') }}"><i class="fa fa-heart"></i>Mes favoris</a></li>
                            <li><a class="dropdown-item signout-head" href="{{ path('app_logout') }}"><i class="fa fa-sign-out"></i> Se déconnecter</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="list-inline-item list_s"><a href="{{ path('login_bdmk') }}" class="btn flaticon-user"> <span class="dn-lg"> <i class="fa fa-user-o"></i> Se connecter</span></a></li>
                {% endif %}

            </ul>
        </nav>
    </div>
</header>


<!-- Main Header Nav For Mobile -->
<div id="page" class="stylehome1 h0">
    <div class="mobile-menu">
        <div class="header stylehome1">
            <div class="main_logo_home2 text-center">
                <img style="width: 120px; height: auto" class="nav_logo_img img-fluid mt20" src="{{ asset('front/images/logo.png') }}" alt="logo">
            </div>
            <ul class="menu_bar_home2">
                <li class="list-inline-item list_s"><a href="#"><span class="flaticon-user"></span></a></li>
                <li class="list-inline-item"><a href="#menu"><span></span></a></li>
            </ul>
        </div>
    </div><!-- /.mobile-menu -->
    <nav id="menu" class="stylehome1">
        <ul>
            <li>
                <a href="{{ path('home_bdmk') }}">Accueil</a>
            </li>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li><a href="{{ path('admin_dashboard_bdmk') }}"><i class="fa fa-dashboard"></i> Tableau de bord</a></li>
                <li><a href="{{ path('list_search_annonces_bdmk') }}"><i class="fa fa-search"></i> Rechercher</a></li>
                <li><a href="{{ path('admin_favoris_liste_bdmk') }}"><i class="fa fa-heart"></i> Favoris</a></li>
                {% if is_granted('ROLE_USER')  %}
                <li><a href="{{ path('edit_profile_admin_bdmk') }}"><i class="fa fa-user-circle"></i> Mon profil</a></li>
                {% elseif is_granted('ROLE_PRO') %}
                <li><a href="{{ path('edit_profile_pro_admin_bdmk') }}"><i class="fa fa-user-circle"></i> Mon profil</a></li>
                {% endif %}
                <li><a href="{{ path('reset_password_bdmk') }}"><i class="fa fa-lock"></i> Mot de passe</a></li>
                <li><a href="{{ path('admin_annnonce_liste_bdmk') }}"><i class="fa fa-bars"></i> Mes annonces</a></li>
                <li><a href="{{ path('app_logout') }}"><i class="fa fa-sign-out"></i> Se déconnecter</a></li>
            {% else %}
                <li><a href="{{ path('login_bdmk') }}"><i class="fa fa-sign-in"></i> Se connecter</a></li>
                <li><a href="{{ path('choice_registration_bdmk') }}"><i class="fa fa-sign-out"></i> S'inscrire</a></li>
                <li><a href="{{ path('login_bdmk') }}"><i class="fa fa-plus-circle"></i> Déposer une annonce</a></li>
                <li><a href="{{ path('login_bdmk') }}"><i class="fa fa-heart"></i> Favoris</a></li>
            {% endif %}
            <li class="cl_btn"><a href="{{ path('admin_annnonce_add_bdmk') }}" class="btn btn-block btn-lg btn-bdmk-mb circle mm-listitem__text"><i class="fa fa-plus"></i> Déposer une annonce</a>
        </ul>
    </nav>
</div>
